# Change Requests

This folder contains live Change Request documents.

All live `CR-*.md` documents live here, including Draft, Review, Accepted and Deprecated Change Requests.

Accepted Change Requests are declared in `specs/accepted/manifest.json`.

Ordinary `SPEC-*.md` documents do not live here. They use `specs/draft/` and `specs/accepted/`.
